import { authFixtures } from './auth';
import { messageFixtures } from './message';
import { userFixtures } from './user';

export const entitiesFixtures = {
  user: userFixtures,
  auth: authFixtures,
  message : messageFixtures,

};
